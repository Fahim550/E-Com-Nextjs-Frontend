/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com'], // Add the domain here
        unoptimized: true,
      },
};

export default nextConfig;
